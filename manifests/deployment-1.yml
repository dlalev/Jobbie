apiVersion : apps/v1
kind: Deployment
metadata:
  name: "jobbiecluster-3ae1"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "jobbiecluster-3ae1"
  template:
    metadata:
      labels:
        app: "jobbiecluster-3ae1"
    spec:
      containers:
        - name: "jobbiecluster-3ae1"
          image: "jobbieaks.azurecr.io/jobbiecluster"
          ports:
          - containerPort: 80