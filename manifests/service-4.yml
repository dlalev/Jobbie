apiVersion: v1
kind: Service
metadata:
    name: "jobbiecluster-5636"
    labels:
        app: "jobbiecluster-5636"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "jobbiecluster-5636"