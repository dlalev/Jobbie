apiVersion: v1
kind: Service
metadata:
    name: "jobbiecluster-8ca5"
    labels:
        app: "jobbiecluster-8ca5"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "jobbiecluster-8ca5"