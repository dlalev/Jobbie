apiVersion : apps/v1
kind: Deployment
metadata:
  name: "jobbiecluster-946e"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "jobbiecluster-946e"
  template:
    metadata:
      labels:
        app: "jobbiecluster-946e"
    spec:
      containers:
        - name: "jobbiecluster-946e"
          image: "jobbieaks.azurecr.io/jobbiecluster"
          ports:
          - containerPort: 80