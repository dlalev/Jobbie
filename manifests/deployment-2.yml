apiVersion : apps/v1
kind: Deployment
metadata:
  name: "jobbiecluster-33d1"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "jobbiecluster-33d1"
  template:
    metadata:
      labels:
        app: "jobbiecluster-33d1"
    spec:
      containers:
        - name: "jobbiecluster-33d1"
          image: "jobbieaks.azurecr.io/jobbiecluster"
          ports:
          - containerPort: 80