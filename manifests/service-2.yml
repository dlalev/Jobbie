apiVersion: v1
kind: Service
metadata:
    name: "jobbiecluster-33d1"
    labels:
        app: "jobbiecluster-33d1"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "jobbiecluster-33d1"